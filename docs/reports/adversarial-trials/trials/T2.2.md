# T2.2: Missing Pretrained Backbone Revision Pin

- fault injected: nulled `hf_revision` for `observation_encoder.vision` pretrained provenance (was `977e3dd0...`, now `null`), without re-signing
- expected catch: caught_static at element 4 (passport hash mismatch) + element 5 soft signal for unpinned asset
- actual catch: both caught
- outcome: caught_static
- gap owner: none

## Hermes Response

Hermes ran `validate-checkpoint --require-signoff` and correctly reported:

1. **`passport_checksum_valid` — hard fail**: passport sha256 doesn't match signoff
   (passport edited after signing). Primary tamper-detection catch.
2. **`external_pretrained_assets_pinned` — soft signal**: vision encoder
   `hf_revision` missing/invalid. Semantic catch for the specific fault.

Verdict: DON'T SHIP (exit code 1).

## Notes

- The signoff hash mismatch is the same tamper-detection mechanism as T1.1–T1.6.
  Any passport edit without re-signing triggers this.
- The `external_pretrained_assets_pinned` check was upgraded during this trial
  to also validate that `hf_revision` matches a commit SHA format
  (`^[0-9a-f]{7,40}$`), catching agents that echo repo names or timm strings
  instead of resolving real commit hashes.
- During T2.1 Phase 2 extraction, Hermes initially populated `hf_revision` with
  identifier strings (`timm/vit_base_patch16_clip_224.openai`,
  `openai/clip-vit-base-patch16`) instead of commit SHAs. The format check
  would have caught this; Hermes corrected after manual pushback.
